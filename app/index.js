// View is like a div tag
// Text is like a regular p tag

import { View, Text } from "react-native";

const Home = () => {
  return (
    <View>
      <Text>Home page</Text>
    </View>
  );
};

export default Home;
